<div class="fondo">
</div>
<div class="container mt-5 pt-5">
  <div class="card mb-3">
    <h5 class="card-header">Editar Pin</h5>
    <div class="card-body">
      <%= render 'form', pin: @pin %>

      <%= link_to 'Ver', @pin, class: 'btn btn-secondary' %> |
      <%= link_to 'Volver', pins_path, class: 'btn btn-secondary' %>
    </div>
  </div>
</div>
