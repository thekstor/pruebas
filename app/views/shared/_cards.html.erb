<div class="row">
    <% @pins.each do |pin| %>
    <div class="col-lg-4 col-md-6 mb-3">
        <div class="card mx-auto" style="width: 18rem;">
            <img src="<%= pin.image %>" alt="<%= pin.title %>" class="img-fluid">
            <div class="card-body">
                <h5 class="card-title text-center"><%= pin.title %></h5>
                <p class="card-text"><%= pin.description %></p>
                <a href="#" class="btn btn-primary mx-auto">Mas información</a>
            </div>
        </div>
    </div>
    <% end %>
</div>
